<!DOCTYPE html>
<html lang="{{ app.request.locale }}">
    <head>
        <meta charset="UTF-8">
        <title>{% block layout_title %}Recruitment{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/layout/layout.css') }}">
        {% endblock %}
    </head>
    <body class="layout__body">
        <div class="layout__flashes">{{ include('layout/flashes.html.twig') }}</div>
        <main class="layout__main">
            {% block layout_main %}{% endblock %}
        </main>
    </body>
</html>
